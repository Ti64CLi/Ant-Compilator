deplacement:
    flip(2, move_forward, turn_right);

turn_right:
    turn(1);
    goto deplacement;

move_forward:
    move(1);
    do {
        pickup(deplacement);
    }
    if (
        food is here
    );
    goto deplacement;