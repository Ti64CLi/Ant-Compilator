var x = 1 in 0 1 ;
var y = 0 in 0 1 ;
# test d'un var_if

var if x == 0
beginthen {

    move(1);
    var if y == 1
    beginthen {
        turn(1);
    } endthen
    beginelse {
        turn(2);
    } endelse

} endthen
beginelse {
    turn(3);
} endelse